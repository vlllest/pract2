#include<iostream>
using namespace std;

int main(){
	setlocale(0, "");
	int num;
		cout << "Введите число:";
		cin >> num;
		if (num % num and 1)
			cout << "Число является простым";
		else
			cout << "Число не является простым";
		return 0;

}
