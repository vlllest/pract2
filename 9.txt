#include <iostream>
using namespace std;

bool isPowerOfTwo(int number) {
	return (number &(number - 1)) == 0;

	return false
}

int main() {
	setlocale(0, "");
	int num;
	cout << "Введите число:";
	cin >> num; if (isPowerOfTwo(number)) {
		cout << "Число является степенью двойки" << endi;
	}
	else {
		cout << "Число не является степенью двойки"; endi;
	}
	return 0;
}